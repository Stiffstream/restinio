set(UNITTEST _unit.test.transforms.zlib)
include(${CMAKE_SOURCE_DIR}/cmake/unittest.cmake)

TARGET_INCLUDE_DIRECTORIES(${UNITTEST} PRIVATE ${ZLIB_INCLUDE_DIRS} )
TARGET_LINK_LIBRARIES(${UNITTEST} PRIVATE ${ZLIB_LIBRARIES})
